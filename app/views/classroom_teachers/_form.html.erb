<%= form_with(model: classroom_teacher) do |form| %>
  <% if classroom_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(classroom_teacher.errors.count, "error") %> prohibited this classroom_teacher from being saved:</h2>

      <ul>
        <% classroom_teacher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3">
    <%= form.label :classroom_id %>
    <%= form.collection_select :classroom_id, Classroom.all, :id, :name, { prompt: true }, { class: "form-select" }  %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :teacher_id %>
    <%= form.collection_select :teacher_id, Teacher.all, :id, :name, { prompt: true }, { class: "form-select" }  %>
  </div>

  <div class="form-group mb-3">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>