<div class="container-fluid">
  <p id="notice"><%= notice %></p>

      <div class="form-group mb-3">
    <p><strong>Classroom:</strong></p>
    <div>
      <%= @classroom_student.classroom_id %>
    </div>
  </div>
  
      <div class="form-group mb-3">
    <p><strong>Student:</strong></p>
    <div>
      <%= @classroom_student.student_id %>
    </div>
  </div>
  
  
  <%= link_to 'Edit', edit_classroom_student_path(@classroom_student), class: "btn btn-primary min-width-btn" %>
  <%= link_to 'Back', classroom_students_path, class: "btn btn-light min-width-btn" %>
</div>